server:
  host: "103.90.75.122:88"
  protocol: "http"

security:
  cors:
    allowed-origin:
      - http://localhost:5173
      - ${server.protocol}://${server.host}

websocket:
  stomp:
    allowed-origin: "${server.protocol}://${server.host}"
  game:
    allowed-origin: "${server.protocol}://${server.host}"